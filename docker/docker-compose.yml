x-ubuntu-version: &ubuntu-version 22.04
x-artifacts-url:  &artifacts-url  https://gitlab.ika.rwth-aachen.de/api/v4/projects/1645/jobs/artifacts/main/download?job=carla:extract_artifacts
x-image:          &image          gitlab.ika.rwth-aachen.de:5050/fb-fi/simulation/carla/scenario_runner:latest

# ==============================================================================

x-build: &build
  dockerfile: ./docker/Dockerfile
  context: ../
  args:
    UBUNTU_VERSION: *ubuntu-version
    ARTIFACTS_URL: *artifacts-url
    CI_TOKEN_USER: $CI_TOKEN_USER
    CI_JOB_TOKEN: $CI_TOKEN_PASSWORD

services:
  run:
    image: *image
    build:
      target: final
      <<: *build
